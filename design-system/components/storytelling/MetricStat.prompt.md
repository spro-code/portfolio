Large editorial number for results and impact.

```jsx
<MetricStat value="87" unit="%" label="of goals on track" delta="12 pts vs. last term" deltaDir="up" />
```

Renders in serif display with tabular figures. Group several in a grid inside a Metrics story.
