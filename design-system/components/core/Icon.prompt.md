Renders a Lucide icon with currentColor. The host page must load Lucide UMD before the bundle.

```jsx
<Icon name="target" size={20} />
```

Use Lucide names. Defaults: size 20, strokeWidth 1.85 (the brand stroke).
