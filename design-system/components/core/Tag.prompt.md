Pill-shaped chip for filters and selectable categories.

```jsx
<Tag selected>Reading</Tag>
<Tag onRemove={() => removeFilter("math")}>Math</Tag>
```

`selected` highlights in brand tint; `onRemove` shows an × button.
