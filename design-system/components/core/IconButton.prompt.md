Icon-only button — toolbar actions, table row actions, close buttons. Always give a `label`.

```jsx
<IconButton icon="more-horizontal" label="More options" />
<IconButton icon="bell" variant="secondary" />
```

Variants match Button; sizes `sm | md | lg`.
